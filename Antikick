local lp = game:GetService("Players").LocalPlayer
local runService = game:GetService("RunService")
local workspace = game:GetService("Workspace")
local starterGui = game:GetService("StarterGui")

local oldGui = lp.PlayerGui:FindFirstChild("MyCustomGui")
if oldGui then oldGui:Destroy() end

local introGui = Instance.new("ScreenGui", lp.PlayerGui)
local blackFrame = Instance.new("Frame", introGui)
blackFrame.Size = UDim2.new(1, 0, 1, 0)
blackFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
blackFrame.BorderSizePixel = 0

local welcomeLabel = Instance.new("TextLabel", introGui)
welcomeLabel.Size = UDim2.new(1, 0, 1, 0)
welcomeLabel.BackgroundTransparency = 1
welcomeLabel.Text = "OPEN SCRIPT"
welcomeLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
welcomeLabel.Font = Enum.Font.GothamBold
welcomeLabel.TextSize = 60
welcomeLabel.ZIndex = 2

task.delay(3, function() 
    if introGui then introGui:Destroy() end 
end)

local function ShowKickWarning()
    local notification = Instance.new("Hint", workspace)
    notification.Text = "Security Alert: A kick attempt has been intercepted and bypassed."
    task.wait(4)
    notification:Destroy()
end

local mt = getrawmetatable(game)
local oldNamecall = mt.__namecall
setreadonly(mt, false)
mt.__namecall = newcclosure(function(self, ...)
    local method = getnamecallmethod()
    if method == "Kick" or method == "kick" or method == "Disconnect" then 
        task.spawn(ShowKickWarning)
        return nil 
    end
    return oldNamecall(self, ...)
end)
setreadonly(mt, true)

local oldPart = workspace:FindFirstChild("???_StablePart")
if oldPart then oldPart:Destroy() end

local refPart = Instance.new("Part", workspace)
refPart.Size = Vector3.new(16, 1, 12)
refPart.CFrame = CFrame.new(-410.2, 18.5, 63.1)
refPart.Anchored = true
refPart.CanCollide = true
refPart.Color = Color3.fromRGB(255, 255, 255)
refPart.Material = Enum.Material.SmoothPlastic
refPart.Name = "???_StablePart"

local MainGui = Instance.new("ScreenGui", lp.PlayerGui)
MainGui.Name = "MyCustomGui"
MainGui.ResetOnSpawn = false

local Frame = Instance.new("Frame", MainGui)
Frame.Size = UDim2.new(0, 180, 0, 40)
Frame.Position = UDim2.new(0.05, 0, 0.4, 0)
Frame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
Frame.BorderSizePixel = 0
Frame.Active = true
Frame.Draggable = true

local StatusLabel = Instance.new("TextLabel", Frame)
StatusLabel.Size = UDim2.new(1, 0, 1, 0)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = "ANTI-KICK: ACTIVE"
StatusLabel.TextColor3 = Color3.fromRGB(0, 255, 100)
StatusLabel.Font = Enum.Font.GothamBold
StatusLabel.TextSize = 14

starterGui:SetCore("SendNotification", {
    Title = "SYSTEM READY",
    Text = "Anti-Kick Shield is fully operational.",
    Duration = 3
})
